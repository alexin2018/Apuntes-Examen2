#include <stdio.h>

int main() {
    char frase[100];
    int contador = 0;
    int longitud=0;

    printf("Ingrese una frase: ");
    gets(frase);

   for (int i = 0; frase[i] != '\0'; i++) {
            contador++;
            longitud++;
   }

        printf ("la frase %s tiene %d letras\n", frase, contador);
        
        printf (" la frase al reves es ");
        for (int i = longitud-1; i>=0; i--) {
             printf("%c", frase[i]);
        }

        
    


    
    return 0;
}